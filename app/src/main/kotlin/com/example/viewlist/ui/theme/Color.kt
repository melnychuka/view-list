package com.example.viewlist.ui.theme

import androidx.compose.ui.graphics.Color

// ── Night sky — Sailor Moon's cosmos, Frieren's eternal road ──
val Background   = Color(0xFF080712)
val Surface      = Color(0xFF1A1732)
val CardBg       = Color(0xFF201D3C)
val CardElevated = Color(0xFF2A2748)

// ── Sakura rose — Cardcaptor Sakura · Sailor Moon ──
val AccentPrimary = Color(0xFFE87FAF)

// ── Viewed · aqua — Sakura's Clow Key glow ──
val AccentEmerald = Color(0xFF5ED4C4)

// ── Planned · lavender — Frieren's ether magic ──
val AccentIndigo  = Color(0xFF9B8EFF)

// ── Gold & silver — Sakura's Star Wand · Sailor Moon's tiara · Frieren's silver hair ──
val StarAmber  = Color(0xFFF5C842)
val StarSilver = Color(0xFFCBC8F0)

// ── Delete rose ──
val DeleteRed = Color(0xFFFF6B8A)

// ── Text ──
val TextPrimary   = Color(0xFFEDE8FF)
val TextSecondary = Color(0xFF9080C0)
val TextMuted     = Color(0xFF4A3F6A)

// ── Structure ──
val BorderColor = Color(0xFF1E1B38)
